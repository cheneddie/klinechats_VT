{
  "schema_version": 1,
  "strategy_id": "MR_BROAD",
  "version": "V3",
  "name": "MR_BROAD_V3",
  "family": "MR",
  "description": "Frozen structural MR training baseline derived from prior MTX research. Do not treat as universal constants or as production edge proof.",
  "node_chain": [
    "AUC_ATTEMPT",
    "MR_REJECTION",
    "MR_CLEAR_RECLAIM",
    "MR_RECLAIM_LEG",
    "MR_LVN",
    "MR_PULLBACK",
    "MR_ENTRY"
  ],
  "value_area": 0.8,
  "session": {"start": "08:45:00", "end": "09:30:00"},
  "auction": {"excursion_pct_value_width": 0.02, "min_points": 2},
  "rejection": {"clear_reclaim_pct_value_width": 0.08, "max_seconds": 60},
  "leg": {"turn_confirmation_points": 8},
  "lvn": {"family": "valley", "depth": 0.55, "tolerance_points": 1, "location": "inside_mid"},
  "entry": {"first_valid_pullback_only": true},
  "stop": {"type": "lvn_buffer", "points": 6},
  "target": {"type": "r_multiple", "r": 0.75},
  "time_stop_seconds": 300,
  "parameter_schema": [
    {"path": "value_area", "kind": "float", "default": 0.8, "scope": "detector", "requires_rescan": true, "minimum": 0.7, "maximum": 0.9, "step": 0.02, "description": "Changes previous-value profile geometry; requires a fresh causal scan."},
    {"path": "auction.excursion_pct_value_width", "kind": "float", "default": 0.02, "scope": "detector", "requires_rescan": true, "minimum": 0.0, "maximum": 0.12, "step": 0.01},
    {"path": "auction.min_points", "kind": "float", "default": 2, "scope": "detector", "requires_rescan": true, "minimum": 1, "maximum": 12, "step": 1},
    {"path": "rejection.clear_reclaim_pct_value_width", "kind": "float", "default": 0.08, "scope": "detector", "requires_rescan": true, "minimum": 0.02, "maximum": 0.2, "step": 0.02},
    {"path": "rejection.max_seconds", "kind": "int", "default": 60, "scope": "detector", "requires_rescan": true, "minimum": 15, "maximum": 180, "step": 15},
    {"path": "leg.turn_confirmation_points", "kind": "float", "default": 8, "scope": "detector", "requires_rescan": true, "minimum": 2, "maximum": 20, "step": 2},
    {"path": "lvn.depth", "kind": "float", "default": 0.55, "scope": "detector", "requires_rescan": true, "minimum": 0.2, "maximum": 0.8, "step": 0.05},
    {"path": "lvn.tolerance_points", "kind": "float", "default": 1, "scope": "detector", "requires_rescan": true, "minimum": 0, "maximum": 4, "step": 0.5},
    {"path": "entry.first_valid_pullback_only", "kind": "bool", "default": true, "scope": "gate", "requires_rescan": true},
    {"path": "stop.points", "kind": "float", "default": 6, "scope": "execution", "requires_rescan": false, "minimum": 3, "maximum": 12, "step": 1, "description": "Execution-only sensitivity can reuse a frozen causal event snapshot."},
    {"path": "target.r", "kind": "float", "default": 0.75, "scope": "execution", "requires_rescan": false, "minimum": 0.5, "maximum": 2.5, "step": 0.25},
    {"path": "time_stop_seconds", "kind": "int", "default": 300, "scope": "execution", "requires_rescan": false, "minimum": 60, "maximum": 900, "step": 60}
  ],
  "research_governance": {
    "baseline_is_frozen": true,
    "production_research_target_floor_r": 1.0,
    "parameter_ranges_are_search_space_hints_not_best_parameters": true,
    "detector_parameter_change_requires_new_research_run": true
  },
  "evidence": {"side_is_aggressor": false, "requires_true_bid_ask_for_orderflow_layer": true}
}
