{
  "name": "fabio-binary-replay-trainer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "klinecharts": "10.0.2"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^8.2.0"
  }
}
