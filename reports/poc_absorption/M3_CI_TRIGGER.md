# M3 Branch-Head CI Trigger

This file intentionally exists to trigger the already-committed `POC Absorption Research CI` workflow from a branch-head push after the workflow definition itself was present.
